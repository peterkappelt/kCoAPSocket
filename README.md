# kCoAPSocket

This is a small java software utility. It acts as a gateway between a TCP socket and a CoAP system.

It is meant as a sub-project for the FHEM Trådfri module and is the base for further optimizations (like real-time updating).